# Dependencies and build output
node_modules/
dist/
coverage/

# VitePress docs site build output and cache
docs/.vitepress/dist/
docs/.vitepress/cache/

# Cloudflare local state
.wrangler/
.dev.vars

# Self-hosted Node host runtime data (SQLite database, env file)
data/
*.db
*.db-wal
*.db-shm
.env

# Per-deployment overrides — your D1 id / instance config, never committed
wrangler.local.jsonc
wrangler.*.local.jsonc

# Editor/OS noise
.DS_Store
Thumbs.db
*.log


# Claude Code local settings (logs and machine-local overrides only)
.claude/logs/
.claude/settings.local.json
