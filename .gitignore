# Dependencies and build output
node_modules/
dist/
coverage/

# Cloudflare local state
.wrangler/
.dev.vars

# Per-deployment overrides — your D1 id / instance config, never committed
wrangler.local.jsonc
wrangler.*.local.jsonc

# Editor/OS noise
.DS_Store
Thumbs.db
*.log
